<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/delete_all_notes"
        android:title="@string/menu_item_delete"
        />

    <item android:id="@+id/searchbar"
        android:title="Searchbar"
        android:titleCondensed="@string/search_hint"
        android:icon="@drawable/ic_baseline_search_24"
        app:showAsAction="always|collapseActionView"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        android:iconTint="@color/bakground_color"
        />

</menu>